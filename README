Build the image:
docker build . --tag jimage

Run the image
docker run -it -p 8080:8080 jimage bash
Visit localhost:8080/checkout  to see the message "You have successfully checked out your shopping cart"

